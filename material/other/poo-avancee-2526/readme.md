# POO avancé

## Pygame

-   Créer un environnement virtuel
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # Sur Windows utilisez `\.venv\Scripts\activate`
    ```
-   Tester un exemple de Pygame
    ```bash
    python -m pygame.examples.aliens
    ```
